3
2x1+3x2+4x3=16
1x1+2x2+1x3=8
3x1+1x2+2x3=13

2x+3y+4z=16
1x+2y+1z=8
3x+1y+2z=13

1x1+2x2+1x3=9
2x1-1x2+3x3=13
3x1+1x2+2x3=16

variable
    num_vars 

function
    equation i
    column x2 (coefficients of variable x2)
    add 1 3
    subtract 1 3
    substitute x2 1 3
    D
    D x1
    D_value
    solve

General Constraints: 
    1- User can input equations can be up to 100 equations.  
    2- All values are of float data types in general case. 
    3- Do not prompt user to enter anything , just read the input directly. 
    4- Output should not include any extra white spaces or any extra texts 
       more than the results.


to get values for any variable if its a matrix or simple var
we will set sequence of functions that store the result in global vars
and then display it to the user

but we need a constant vars like num_vars, and matrix that has coeff's (then we could regenerate the original equations)

sequence to solve
    get matrix as input
    get D_value
        extend matrix (generate D matrix)
    get D_x1
        extend matrix (generate D matrix)
        get extended matrix
        insert constants in the extended matrix
    solve
        get_positive_multiplication
        get_negative_multiplication
